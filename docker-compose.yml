version: '3.9'


services:
  app:
    build: 
      context: .
    container_name: recipe-app-api
    ports:
      - "8000:8000"
    volumes:
      - ./app:/app
    command: >
      sh -c "python manage.py runserver 8000"

